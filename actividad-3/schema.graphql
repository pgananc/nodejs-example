type Query {
  employees: [Employee]
  employee(id: String!): Employee
  deparment(departmentId: String!): [Deparment]
}

type Employee {
  name: String!
  age: Int
  status: Boolean!
}

type Deparment {
  name: String!
  description: String
}
